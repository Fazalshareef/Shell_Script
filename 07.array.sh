#!/bin/bash

DEVOPS=("Linux" "AWS" "Ansible" "Jenkins" "Terraform" "Docker" "Kubernetes")

echo "First thing need to learn is: ${DEVOPS[0]}"

echo "Second thing need to learn is: ${DEVOPS[1]}"

echo "Fifth thing need to learn is: ${DEVOPS[4]}"

echo "things to learn while preparing the devops role: ${DEVOPS[@]}"